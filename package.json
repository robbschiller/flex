{
  "name": "src",
  "description": "flex-site",
  "repository": "git://github.com/.git",
  "version": "0.0.0",
  "main": "build.js",
  "dependencies": {
    "metalsmith": "1.x"
  },
  "devDependencies": {
    "metalsmith": "^1.0.1",
    "metalsmith-autoprefixer": "^0.1.2",
    "metalsmith-sass": "^0.5.0"
  }
}
